/* Paste your Firebase web config here after creating a free project.
   Realtime Database must be enabled. Rules example:

   {
     "rules": {
       "trips": {
         "$roomId": { ".read": true, ".write": true }
       }
     }
   }

   Room codes are random — only people with the link share data.
   Another couple creates their own room = completely separate lists.
*/
window.FIREBASE_CONFIG = null;
